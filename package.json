{
  "name": "cloudflare-workers-quickstart",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy src/app.ts --minify --compatibility-flags='nodejs_compat' --tsconfig='tsconfig.json'",
    "dev": "wrangler dev src/app.ts --compatibility-flags='nodejs_compat'"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.6.3",
    "@cloudflare/next-on-pages": "1",
    "@cloudflare/workers-types": "^4.20240405.0",
    "@types/node": "^20",
    "typescript": "^5",
    "wrangler": "^3.53.1"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.2.2",
    "hono": "^4.2.1",
    "zod": "^3.23.8"
  }
}
